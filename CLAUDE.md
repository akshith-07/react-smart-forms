Create an enterprise-grade NPM package called "react-smart-forms" - a visual React form builder with drag-and-drop interface and AI-powered validation using Google Gemini API.

Features:
- Drag-and-drop form builder component
- 20+ pre-built field types (text, email, select, radio, checkbox, file upload, date, etc.)
- Multi-step form wizard
- Conditional logic (show/hide fields based on values)
- AI validation (validate inputs using Gemini API - check if email is professional, if text is appropriate, etc.)
- Real-time validation with custom error messages
- Form schema export/import (JSON)
- Multiple themes (default, Bootstrap, Tailwind, Material)
- Responsive design
- Accessibility (ARIA labels, keyboard navigation)
- TypeScript support with full type definitions
- Form submission hooks
- Analytics integration (track form views, completions, field interactions)
- Extensive documentation with examples
- Storybook for component showcase
- Unit tests with Jest and React Testing Library

Tech Stack:
- React 18+
- TypeScript
- react-dnd (drag and drop)
- Zod (schema validation)
- Google Gemini API
- CSS-in-JS (styled-components or emotion)

Provide complete production-ready code with proper NPM package structure, README, examples, tests, and publishing guide.
